Services:- 
kubernets have 4 kinds of Services:-
1. ClusterIP (Default) :- Exposes the Service on a cluster-internal IP. Choosing this value makes the Service only reachable from within the cluster.

2. NodePort :- Exposes the Service on each Node's IP at a static port 
(NodePort). A ClusterIP Service, to which the NodePort Service routes, is automatically created. You’ll be able to contact the NodePort Service,
 from outside the cluster, by requesting <NodeIP>:<NodePort>.

3. LoadBalancer :- Exposes the Service externally using a cloud provider's load balancer.

4. ExternalName :- Maps the Service to the contents of the externalName field (e.g. foo.bar.example.com), by returning a CNAME record with its value.
 No proxying of any kind is set up.

Give me examples so i understand and give me story also so i easily understabnd
Sure! Let's break down each type of Kubernetes Service with examples and a story to help you understand better.
### Story:
Imagine you are the manager of a large amusement park called "KubePark". In this park
, you have different attractions (applications) that visitors (users) want to access. To manage the flow of visitors to these attractions,
 you set up different types of entry points (Services) based on how visitors can access them.

### 1. ClusterIP (Default)
- Example:
```yaml
apiVersion: v1
kind: Service
metadata:
    name: 
