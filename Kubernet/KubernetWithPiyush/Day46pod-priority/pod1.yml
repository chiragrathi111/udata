apiVersion: v1
kind: Pod
metadata:
  name: high-priority-nginx
spec:
  containers:
  - image: nginx
    name: nginx
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        memory: "128Mi"
        cpu: "500m"
  priorityClassName: high-priority
