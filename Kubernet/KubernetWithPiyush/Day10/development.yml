apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  labels:
    app: my-app-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      name: my-app-ns
  template:
    metadata:
      labels:
        name: my-app-ns
    spec:
      containers:
      - name: my-container
        image: nginx:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80