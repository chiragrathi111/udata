kind: Service
apiVersion: v1
metadata:
  name: my-service
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    name: my-app    # This should match the label of the pods you want to expose
  type: ClusterIP  # Other options: NodePort, LoadBalancer, etc.